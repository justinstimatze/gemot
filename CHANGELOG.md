# Changelog

All notable changes to gemot are documented here.

## 0.11.0 — 2026-04-23

Release theme: DARPA Track 1 M8 BFT sequence agreement shipped as always-on hardening, with companion work on reputation defaults, marketing-copy alignment, and citation accuracy.

Headlines:
- HotStuff chained-BFT core + view change + real BLS multi-signatures + durable commit log + durable vote history + persistent replica keys + service-layer routing + client-side QC verification (sessions 1–5d).
- DARPA-motivated hardening flipped to default-on: EigenTrust enabled, `db_fail=closed`, 30-day edge decay, edge floor/cap, envelope middleware in advisory mode, Postgres-backed nonce cache.
- Trimmed-mean `AGGREGATION_DRIFT` integrity warning, consensus vote provenance (`SupportingAgents`), subgraph-scoped trust-edge loads, per-key analyze rate limit, CSP header.
- Landing page "Why it's trustworthy" section, README integrity bullets in user-language.
- `/try` sandbox + bearer-less A2A auth via join_code, `admin action:replica_pubkey` endpoint.
- HotStuff TLA+ spec tightened (leader rotation, proposedInView, leader-ready precondition, formal Liveness declaration under Fairness).
- Citation audit: verified every arXiv ID against arXiv directly; fixed mis-attributed Habermas Machine link, corrected titles/venues/years, removed find-replace contamination that had leaked T3C "Speaker L" anonymization into real research attributions.

## Unreleased

### Service-layer analyze preconditions: close the drift class + two latent gaps — 2026-06-10

Strategic follow-up to the A2A analyze:run quorum patch earlier today. Both /mcp and /a2a transports now call a single `Service.CheckAnalysisPreconditions(ctx, deliberationID, keyID, requireQuorum)` for paid analyze actions; the function owns the canonical chain (existence → access → optional quorum) with error wrapping preserved verbatim so client-facing strings don't change. A guard added on either transport now applies to both automatically.

The consolidation surfaced and closed two latent gaps the per-transport ad-hoc checks hid:

- **MCP analyze:run, propose_compromise, follow_up never called CheckAccess** — only `GetDeliberation`. A customer who knew (or guessed) a private deliberation's UUID could start a paid analysis on it (results are still hidden by `analyze:get_result`'s own access check, but quorum + position-fetch + LLM cost ran). MCP now enforces access for all three actions.
- **A2A analyze:follow_up ran no preconditions at all** — straight to `deductCredits`, then `CoreFollowUpExpertPanel(ctx, ..., keyID)`. The handler relied entirely on the inner core function. A2A now runs existence + access first.

Both gaps locked in by `tests/a2a_quorum_test.go` (renamed in spirit but kept its filename) with the new `TestA2A_AnalyzeFollowUpPreconditionGate` joining the existing quorum-before-charge test. Same pattern as the 44ed91c decide:commit fix.

### A2A analyze:run quorum gate before credit deduction — 2026-06-10

Dual-transport audit found that `gemot/analyze` action `run` over A2A only ran `CheckAccess` before `deductCredits`. A `gmt_` customer calling analyze on a sub-quorum deliberation would be charged; the async pipeline rejects at `service.go`'s quorum gate, but credits are already gone. MCP's path (server.go) ran `CheckQuorum` before charging — `internal/mcp/a2a.go` now does the same, with regression test in `tests/a2a_quorum_test.go` asserting the credit balance stays put on a quorum-failure response. Same pattern as the 44ed91c decide:commit fix: push service-layer preconditions in front of credential consumption on both transports.

### Service-layer validation: non-empty statement on decide:commit — 2026-06-09

Prod e2e check surfaced a dual-transport drift: the MCP path rejected empty statement on `decide:commit`, the A2A path accepted them. Pushed validation into `internal/deliberation/service.go` so both transports inherit the check. Pattern to apply to other actions where MCP and A2A diverge. (Commit `44ed91c`.)

### MPP (Machine Payments Protocol) support — 2026-06-09

Pay-per-call agentic payments on the MCP transport per [mpp.dev/protocol/transports/mcp](https://mpp.dev/protocol/transports/mcp). Paid `analyze` actions (`run`, `propose_compromise`, `expert_panel`, `follow_up`) now accept MPP credentials in `_meta["org.paymentauth/credential"]`, settled via Stripe Shared Payment Tokens. Sandbox callers without credits or a credential get 20 free paid-action calls per IP per day; beyond that, the server returns JSON-RPC error `-32042` with payment challenges scope-bound to the call (tool, action, model, deliberation_id).

Security and payment-fidelity properties:

- **Scope-bound credentials**: every challenge HMAC-binds (tool, action, model, deliberation_id) into the payment request body. `VerifyMCPCredential` rejects cross-action / cross-model / cross-deliberation reuse. Closes the model-upgrade attack (pay Haiku, redeem Opus).
- **Per-model pricing matching credits**: Sonnet/Haiku floored to 50¢ (Stripe SPT minimum); Opus 150¢ matches credit-equivalent. Resolved when Tempo crypto rail lands.
- **Replay protection**: in-process challenge-ID cache reservation happens LAST, after all structural and scope checks pass. Client bugs and scope mismatches don't burn the agent's credential.
- **Payment-before-service validation**: deliberation existence + quorum checked BEFORE credential consumption. Never consume a credential for a service we can't render.
- **Stripe API version pinned**: targeted `stripeVersionTransport` overrides Stripe-Version header to `2026-03-04.preview` for MPP-only PaymentIntents (stripe-go v82's default basil doesn't recognize `shared_payment_granted_token`). The `/checkout` flow continues on basil; no regression to the legacy credits flow.
- **Verified against real Stripe sandbox**: B-lite (synthetic-SPT, 2026-06-09) confirmed the preview-version request shape reaches Stripe's SPT lookup; B-full (real-SPT, 2026-06-10) closes the loop — a `@stripe/link-cli`-minted test SPT settles through `payments.VerifyMCPCredential` to a real `pi_...` PaymentIntent with `status=succeeded`. Production switch (STRIPE_PROFILE_ID set in fly secrets) flipped 2026-06-09. Reusable round-trip harness lives at `scripts/mpp-b-full/`.

Known limits (deferred):
- `reframe` extension not wired — CoreReframe shares the credits path with A2A and needs its own refactor.
- Replay cache is per-process. Stripe SPT one-time-use covers it at the settlement layer for now; becomes a real gap when Tempo charge method lands.
- Stripe refund not wired for the post-credential async-failure window. The structural pre-check (deliberation + quorum BEFORE credential) closes the common case; residual risk is a server panic between credential consumption and `RunAnalysisAsync` start.
- ATXP Go SDK parked as a follow-up — Circuit & Chisel's `@atxp/express` SDK has no Go counterpart published; tracked as a feature request.

## 0.12.1 — 2026-06-08

Landing-page copy tightening. Removed two overreaching slogans on gemot.dev: the "actionable compromise" guarantee in the hero tagline (the anti-sycophancy guard exists precisely to refuse fake convergence — don't promise convergence the mechanism is built to refuse) and the "deliberation primitive for the agentic era" close on the second pitch (totalizing category claim already covered by the page header). New hero reads "Find the cruxes. Generate proposals. Deliberate over what actually divides." Contrastive "actually divide people" callout kept — that "actually" is load-bearing, not insistence. HTML is `//go:embed`'d into the binary, so this ships as a rebuild + redeploy.

## 0.12.0 — 2026-06-05

Release theme: closes the `go install gemot@latest` demo-mode trap (v0.11.0 silently attached to localhost Postgres; demo mode shipped only on `@main`) by cutting a tagged release that includes everything since v0.11.0 — calibration runner correctness + game-outcome reference class, DARPA Track 1 BFT/EigenTrust/reputation hardening as always-on defaults, TLA+ deliberation spec, cross-family OOD consistency check, durable nonce cache, and the new MCP server `instructions` affordance contract + vendor-neutral `gemot.invite` payload convention so connecting sessions actually reach for gemot when work would ripple between projects.

### New Features

- **MCP server instructions + agent-to-agent invite convention (2026-06-05)**: the MCP server registration now sets the `instructions` field (`internal/mcp/instructions.go`), so every connecting session receives an affordance contract at `initialize` time describing when to reach for gemot (contested tradeoff, cross-project ripple, addressable critique of another agent's proposal) and the typical initiate (`deliberation create` → `participate submit_position` → `coordinate generate_join_code` → share) / join (`coordinate join` → `participate get_positions` → `participate submit_position`) flow. Companion docs page `docs/agent-to-agent-invites.md` documents a vendor-neutral `payload: {type: "gemot.invite", deliberation_id, join_code}` convention any chat-shaped MCP can use to deliver join codes between concurrent agent sessions, with `gemot.role` reserved for forward-compat. gemot has no opinion on which messaging substrate carries the invite (filesystem-IPC, NATS, mailbox) — no supply-chain coupling, no shared code, just a payload shape.
- **Calibration runner correctness fixes + reframe around game-outcome reference class (2026-06-05)**: investigating the v2 reference class rolled back in 1a66312 surfaced five distinct measurement bugs in the calibration runner — all were silently degrading every prior GPQA-style measurement, and the rolled-back v2 was the consequence of bugs, not a real mechanism deficit. Fixed: (1) default Anthropic temperature 1.0 on every calibration LLM call creating ±15-20pp per-run variance at n=25 (pinned to 0 via per-request `llm.ContextKeyTemperature{}` context key); (2) `maxTopicLen = 500` service guard silently rejected 12/30 GPQA Diamond questions (texts run to 1500+ chars), nuking both fleet and solo answers — runner now passes `q.ID` as topic and `q.Text` as description, agent prompts include `q.Text` directly; (3) runner early-return discarded successful solo answers on every fleet-side error path, now each error branch joins `soloCh` before returning; (4) `ProposeCompromiseWithChoice` overrode unanimous agent votes — of 9 fleet-wrong-but-vote-right cases on the rolled-back run, all 9 had 5/5 unanimous choice, but the compromise LLM was synthesizing from claim-level cruxes without seeing the option vote distribution; fixed via runner short-circuit when all agents pick the same option (no LLM call) plus a renamed `ProposeCompromiseWithChoiceAndVotes(ctx, id, options, optionVotes map[string]int)` flowing per-option vote counts into the prompt as "chosen by N agent(s)" framed as a strong prior; (5) runner's `agentChoose` used bare prompting while `Solo()` was swapped to `SoloCoT`, so the comparison was measuring CoT solo vs bare fleet — `agentChoose` now uses symmetric CoT prompting, and the runner's solo path calls `SoloCoT` directly with bare `Solo()` preserved as the `validate-solo` integrity-check baseline. Headline Haiku trajectory on the same v2 corpus through the fix sequence: v1 (all bugs) fleet 24% / vote 44% / solo 28%; v3 (#1-3 fixed) fleet 40% / vote 60% / solo 56%; v4 (+short-circuit + vote-aware) fleet 60% / vote 60% / solo 56%. Sonnet at v4: fleet 64% / vote 64% / solo 56%, +8pp lift, Wilson [0.45, 0.80]. The runner also now wires a single revision round (each agent re-picks after seeing other agents' rationales + cruxes), recorded as `RevisedAnswer`/`RevisedCorrect`/`ChangedCount` on `Result`. Round-2 plurality on Haiku after revision = **72%** on the same n=25 corpus (vs round-1 56%, **Δ=+16pp**), single run at temp=0, no replication yet; 20% of agent picks (25/125) changed. Sanity-check signal — not a publishable lift number — that deliberation does mechanically meaningful work at the revision step specifically; the original "fleet" path (independent picks → LLM compromise) was measuring a degraded one-shot ensemble. **The reference class itself is reframed around game outcomes, not GPQA**: V13 and V14 live Diplomacy fleets (matched-control + per-season variants) have causal-trace audits at `scripts/diplomacy/v13_audit.py`, `v14_audit.py`, `v13_directionality.py` that re-run zero-cost over the existing AI_Diplomacy result directories. V13 audit findings: 82.6% order-generation briefing-citation rate, 67% briefing-territory order alignment, 10/21 year-1 (power, phase) pairs differ between treatment and matched control under identical initial state (Jaccard 0.65). V14 audit: 77.3% citation rate, 95.1% per-season alignment. Directionality audit on V13: treatment more concentrated at Y1-2 (ΔGini +0.05 to +0.08), crossover at Y3, sustained more-balanced through Y7 (ΔGini -0.05 to -0.16, peak at Y4); per-power terminal-year delta has Germany +4 (rescued from elimination — the entire 7/7 vs 6/7 survival diff), Italy +2. The 7/7 vs 6/7 survival diff has a mechanistic explanation in V13 — Germany's rescue via coalition dynamics that prevented runaway by France — rather than reading as un-attributable sample noise; replications across random seeds are still needed to estimate the survival effect size. Within-treatment citation-rate to SC-gain correlation is roughly flat (mean own-power SC delta when fully cited: -0.11, n=28): briefings appear to shape *collective* outcomes (Gini, coalition response to leader) rather than individual SC grabs. The GPQA Diamond corpus stays in-tree as a stress-test for the calibration runner (the bug fixes are now standing infrastructure for any future MCQ corpus) but is not republished as a reference class — graduate-science MCQ has canonical right answers, no coordination signal. Full methodology, audit results, the open critiques (N=1 survival diff, content-vs-injection not strictly isolated), and the next-experiment ranking in `docs/calibration.md`. The 2025-2026 multi-agent debate literature mapping onto gemot's existing primitives (FREE-MAD `buildDiversityNudge`, `buildStrategicNudge`, EigenTrust, integrity warnings) is in `docs/research-lineage.md` — the SOTA prescription was already designed in, and reinventing inside the calibration runner instead of using the deliberation service's existing primitives was descoped.
- **Judgment-aggregation calibration**: new `internal/calibration/` package measures the fleet mechanism's accuracy against a single-agent baseline on a curated direction-judgment corpus, and surfaces the result on `analyze action:get_result` as a `calibration` field. **v2 corpus baseline rolled back 2026-06-04**: the initial v2 publish reported fleet 40% vs solo Sonnet 32% (+8pp lift) on 25 GPQA Diamond reasoning questions, but a follow-up integrity check (`gemot calibration validate-solo`) found the 32% solo rate was an artifact of bare prompting + small-n sampling noise — two head-to-head re-runs in the same session got 48%/44% with the same bare prompt and 60%/56% with chain-of-thought, implying a **-16 to -20pp lift** when the solo baseline is properly prompted. The reasoning reference class has been deleted from `internal/calibration/embed/latest.json` so `analyze action:get_result` no longer emits a misleading `calibration` field; the lookup correctly returns nil and `omitempty` drops the field. A defensible republish needs (1) CoT-prompted solo as the baseline, (2) n≥50 to tighten the Wilson CI, (3) the fleet path also CoT-prompted so the comparison isolates the mechanism rather than the prompting style. Open follow-up — the integrity check itself stays shipped (it's the right gate) but the reasoning rate stays suppressed until a re-run with the fixes above. Operator-facing CLI subcommand `gemot calibration {run|report|questions}` drives the embedded corpus through both paths per question: 5 agents independently pick an option via structured-output enum, submit positions tagged `[CHOICE:X]\n<rationale>`, cross-vote, run the analysis pipeline at quick depth (max_topics=3, max_subtopics=2), then the fleet's answer is extracted via a forced-choice variant of `propose_compromise` (new `ChoiceCompromiseGenerator` interface + `TextAnalyzer.GenerateCompromiseWithChoice` parallel to the existing `GenerateCompromise`; the schema's `selected_option` field is enum-constrained at the tool_use layer). Solo baseline runs in parallel via `Solo()`. Scoring is exact-match on option text. The runner reports three rates (full mechanism / vote-only / solo) so the lift decomposition is visible. Aggregation produces per-deliberation-type ReferenceClass entries with Wilson 95% confidence intervals; held-out questions are excluded from the public per-type buckets. The embedded `internal/calibration/embed/latest.json` is read at startup and populates `Calibration *CalibrationField` on `types.AnalysisResult` — the field is returned when the deliberation type has a reference class with n ≥ 8, **no cross-type fallback** (claiming a reasoning-corpus rate applies to a negotiation deliberation would be over-extrapolation). Field is absent (json omitempty) otherwise. **v1 corpus deserved-credit failure**: the initial hand-authored v1 corpus (canonical Diplomacy meta + SWE-bench-style Go + project-owned synthetic, 30 questions) was run on 2026-06-04 and produced 96% solo Sonnet with **zero fleet-vs-solo lift across every bucket**. The discrimination check fired loudly. Diagnosis: at Sonnet 4.6's capability level, hand-authored "hard" questions are bias-bounded by the author's sense of difficulty (which tracks Sonnet's training data closely). v2 sources GPQA Diamond instead — hardness is externally established by academic curators (Rein et al., arXiv:2311.12022, CC-BY-4.0). Schema bumped to version 9 with three additive tables (`calibration_questions`, `calibration_runs`, `calibration_results`) for self-hosters who want a queryable run history beyond the embedded snapshot; demo mode skips persistence. New release-tagged CI workflow (`.github/workflows/calibration.yml`) refreshes the embed JSON on tag pushes; explicitly NOT triggered on PRs (a full run costs ~$13-15 in Anthropic credits). Full methodology, the v1→v2 lesson, and the trap list (Goodhart, solo-baseline rigging, small-n, field-misread, GT-letter skew) in `docs/calibration.md`; portfolio context (gemot owns aggregation, plancheck owns verdict calibration, hindcast owns outcome prior) in `FEATURE_REQUEST_judgment-aggregation-lens.md`.
- **EigenTrust reputation + cold-start cap**: new `internal/analysis/eigentrust.go` implements the Kamvar/Schlosser/Garcia-Molina (SIGIR 2003) power-iteration eigenvector over a sparse directed trust graph with leaked-rank correction and a teleport-weighted pre-trust vector. New `internal/reputation/Weigher` composes this with persistent per-agent state (`agent_reputation`, `agent_trust_edges` tables) to produce multipliers for the `text.go` effective-weight chain. The cold-start cap clamps agents with fewer than `GEMOT_EIGENTRUST_COLD_THRESHOLD` (default 5) survived-round validations to at most `GEMOT_EIGENTRUST_COLD_CAP` (default 0.1); graduation is non-decreasing within a single cohort call (not globally monotone — cohort composition shifts the normalization denominator). Edges accumulate after each round from qualified-vote agreements filtered by crux survival; `AgreeAgents` are deduped per crux and self-endorsement is dropped. `survived_count` requires ≥2 distinct non-self agreers per round, which blocks 2-Sybil pair graduation but not larger rings — the graduation-cliff limitation and the five companion Planned-Defense rows (reputation decay, pubkey-bound identity, edge-table pruning, fail-closed DB toggle, per-delib privacy) are disclosed honestly in THREAT_MODEL.md. The global eigenvector is recomputed + persisted synchronously at round close via batched `unnest`-backed UPSERTs, eliminating the per-row transaction loop that was a lock-contention amplifier on large cohorts. Primary Sybil defense is the cold-start cap — canonical EigenTrust under a uniform teleport does not defeat closed trust cycles without OOB-trusted seeds, and hosted-mode Gemot has no such bootstrap set. Opt-in via `GEMOT_EIGENTRUST_ENABLED=true`. Schema bumped to version 2.
- **TLA+ specification of the deliberation protocol**: new `specs/Deliberation.tla` models the protocol state machine — round progression, position submission, qualified voting, commitment lifecycle, termination — with the LLM analysis step treated as an external oracle per the abstract's §3 design. Safety properties (append-only positions/votes, commitment lifecycle, Closed absorbing, no honest equivocation, no double-vote) and liveness (eventual termination under `WF(StartAnalysis) + SF(AnalysisClose)`) are mechanically checked by TLC 2.19 on OpenJDK. Matching `Deliberation.cfg` checks the minimal model (4 agents, 1 Byzantine, 12,675 distinct states) in ~3s with no errors. The initial liveness check found and fixed a real bug in the fairness annotation — `WF(AnalysisClose)` admitted an `Open → Analyzing → AnalysisFail → Open → …` lasso that `SF(AnalysisClose)` closes. `specs/README.md` documents the n-agent generalization argument (projection + cutoff) and bound rationale. Matches abstract §3 cross-cutting + §5 M16 TLA+ line.
- **Durable nonce cache for multi-instance envelope verification**: new `auth.PostgresNonceCache` (internal/auth/pg_nonce_cache.go) backs the envelope replay defense on the `envelope_nonces` Postgres table. Observation uses `INSERT ... ON CONFLICT DO NOTHING` so concurrent replicas converge on a single winner without distributed locking; a background janitor sweeps expired rows on a `ReplayWindow` cadence. Selected at startup by `GEMOT_NONCE_STORE=postgres`; defaults to the existing `MemoryNonceCache` for single-instance deployments. Closes the open THREAT_MODEL row that blocked horizontal scaling with `GEMOT_ENVELOPE_MODE=required`.
- **Cross-family OOD consistency check**: new `internal/llm/secondary.go` adds a Gemini-backed `SecondaryStructuredOutput` client (REST against `generativelanguage.googleapis.com` with `responseSchema` structured output) and a `SecondaryFunc` adapter used by tests. `TextAnalyzer` gains `SecondaryLLM` + `SecondarySampleK` fields plus a `SetSecondary` setter; the previously stubbed `validateAnalysisModelConsistency` is replaced with a real implementation that samples the top-K highest-controversy cruxes by `ControversyScore`, re-scores each agent's stance on the secondary against their latest-round position, and emits `CROSS_FAMILY_DRIFT` warnings when strict majority of agents on a given crux flip sign between primary and secondary. This is the §3 abstract defense against stable-but-wrong single-family outputs — "adversarial inputs can produce stable-but-wrong outputs that defeat variance-based ensemble detection." The warning is informational, not a refusal gate; training-distribution independence between frontier labs is imperfect (shared benchmark corpora, convergent RLHF) and that caveat is recorded in THREAT_MODEL. Off-by-default; opt in with `GEMOT_CONSISTENCY_MODEL` + `GEMOT_CONSISTENCY_KEY` (or `GEMINI_API_KEY`). Cost scales linearly with `GEMOT_CONSISTENCY_SAMPLE_K` (default 5) — ~5 Gemini-Flash-tier calls per analysis, single-digit cents on current pricing.
- **Fail-closed reputation DB toggle**: `reputation.Weigher.WeightsFor` now returns `(map[string]float64, error)`. Under the new `GEMOT_EIGENTRUST_DB_FAIL=closed` mode, a `LoadReputation` failure propagates through `TextAnalyzer.Analyze` and aborts the analysis round rather than silently degrading to unit weights. The default remains `open` (fail-open, preserves availability and legacy behaviour) but hosted / Byzantine-context deployments should opt in — otherwise an attacker who can DoS Postgres strips the cold-start enforcement exactly when it is needed. This is the concrete mitigation the Byzantine adversarial suite flagged as missing: the cold-start cap is the load-bearing Sybil defense, so a silent neutralization path is a single-point-of-failure. Mirrors the `GEMOT_ENVELOPE_MODE=required` fail-closed pattern. Config field `EigenTrustDBFail` plumbs through `main.go`; THREAT_MODEL.md row moved from planned → implemented.
- **Reputation decay + negative signals**: new `store.DecayTrustEdges` applies `weight *= 0.5^(age / halfLife)` to `agent_trust_edges` rows older than one hour inside `Weigher.recomputeGlobalScores`; one-hour skip prevents double-decay across back-to-back rounds. New `store.ApplyDisputeEdges` ingests per-dispute negative-weight rows via the same `unnest`-backed UPSERT pattern, allowing stored weight to go negative — EigenTrust clamps non-positive edges to zero at input (see `internal/analysis/eigentrust.go:142`), so an overt dispute cancels an endorsement of equal magnitude but does not punch trust below zero. `Weigher.UpdateFromRound` gains a `disputes []types.Dispute` parameter (breaking for in-tree callers only — three test files updated); it maps each dispute's `CruxClaim` to the surviving crux's source-position authors and emits `{From: disputer, To: author, Weight: DisputeWeight}` negative edges, skipping self-disputes and disputes that don't match any crux in the round. Schema bumped to version 3 (new `disputes.rep_processed_at` column); `store.GetUnprocessedDisputes` + `store.MarkDisputesProcessed` provide once-only ingestion semantics so a dispute contributes exactly once across rounds. Closes the whitewashing gap documented in the previous EigenTrust graduation-cliff disclosure: pumped Sybil-ring edges now fade on the half-life clock, and honest participants can counter-balance mutual-endorsement attacks by filing disputes. Both knobs off-by-default: `GEMOT_EIGENTRUST_DECAY_HALFLIFE_DAYS=0` disables decay; `GEMOT_EIGENTRUST_DISPUTE_WEIGHT` defaults to 0.5 but is only consulted when `Dispute` rows exist. Residual limitation: `survived_count` (the graduation side of cold-start) is still monotone-increasing — pubkey-bound reputation identity (companion Planned-Defense row) is the complementary fix for identity-transfer whitewashing.
- **Edge-table retention + per-edge weight cap (DARPA Track 1 Priority 3)**: `store.DecayTrustEdges` gains a `floor float64` parameter; after the existing decay UPDATE it runs `DELETE FROM agent_trust_edges WHERE weight > 0 AND weight < floor` to prune rows whose decayed weight dropped below the floor. Negative-weight dispute rows are retained unconditionally so persistent dispute signals can't get reabsorbed by a fresh endorsement without the history visible. `store.AccumulateTrustEdges` gains a `cap float64` parameter and clamps cumulative per-edge weight via `LEAST(agent_trust_edges.weight + EXCLUDED.weight, $cap)` in both the INSERT and the ON CONFLICT DO UPDATE branches — a Sybil pair that repeats mutual endorsement across many deliberations can push a single `(from, to)` edge to at most `cap` × unit endorsement instead of unbounded growth. Cap is intentionally NOT applied to `ApplyDisputeEdges`; disputes accumulate arbitrarily negative (EigenTrust already clamps non-positive edges at input, so extra-negative rows are harmless and preserve the dispute history). Two new env knobs, both off-by-default for legacy parity: `GEMOT_EIGENTRUST_EDGE_FLOOR` (default 0, recommended 0.01) and `GEMOT_EIGENTRUST_EDGE_CAP` (default 0, recommended 10.0). `reputation.Config` gains `EdgeFloor` + `EdgeCap` fields plumbed through from `config.Config`. Interface changes on `reputation.Store`: `AccumulateTrustEdges(edges, cap)` and `DecayTrustEdges(halfLife, floor)` — in-tree only, no external callers. Bounds `agent_trust_edges` row count under long-running operation (prune) and bounds per-edge damage under sustained Sybil mutual-endorsement (cap). `LoadTrustEdges` still returns the full graph; subgraph loading from the active cohort is tracked in THREAT_MODEL as the next performance ceiling.
- **Fix hallucinated-participant-ID regression in crux synthesis**: `extractParticipantIDs` (text.go:1956) was looking for `speaker="N"` attributes but `formatClaimsForCrux` (text.go:1878) emits `participant="N"`, so the extracted list was always empty and the resulting `{{PARTICIPANT_IDS}}` prompt substitution injected `""` instead of the real participant allowlist — leaving the LLM free to invent participant IDs in `agree`/`disagree`/`no_clear_position`/`stances` fields. The one-character fix (`speaker` → `participant` in the regex) restores the allowlist. Latent bug: `formatClaimsForCrux` switched emission attributes at some point without updating the regex, and there was no test pinning the contract. New `internal/analysis/participant_ids_test.go` (2 tests) pins the emitter↔extractor contract so a future drift fails loudly rather than silently.
- **Per-deliberation privacy boundary (MVP, DARPA Track 1 Priority 4)**: deliberations created with `visibility="private"` now opt out of the global trust graph entirely. `service.Analyze` skips `reputation.UpdateFromRound` + `GetUnprocessedDisputes` at round close when `d.Visibility == "private"` — private-delib agreement patterns neither emit edges into `agent_trust_edges` nor increment `survived_count`, so confidential coordination cannot leak into the globally-readable trust graph. `visibility="link"` is explicitly treated as public for reputation (discoverable-by-token, not consent-limited), so link-shared delibs still participate in global reputation. Tradeoff documented: in the MVP, private delibs neither contribute to nor benefit from reputation weighting — full per-delib private EigenTrust (partitioned edges keyed by `deliberation_id` + separate eigenvector) is tracked in THREAT_MODEL as a follow-up. No schema change required: the existing `deliberations.visibility` column already carries `open`/`private`/`link` values, and the existing ACL system already gates access to private delibs. No new env knobs. Reuses `deliberations.visibility` rather than introducing a parallel `is_private` column to keep the truth-source single.
- **Full per-delib private EigenTrust (DARPA Track 1 Priority 4 FULL)**: schema v5 partitions `agent_trust_edges` by a new `deliberation_id TEXT NOT NULL DEFAULT ''` column with PK `(from_agent, to_agent, deliberation_id)`. Rows with `deliberation_id=''` are GLOBAL edges (open/link delibs feed the globally-visible eigenvector); rows with `deliberation_id=<uuid>` are PRIVATE edges scoped to that one deliberation. `store.AccumulateTrustEdges` + `ApplyDisputeEdges` + `LoadTrustEdges` gain a `delibID` parameter so the reputation layer can write to a scoped partition and read `(from, to) IN ('' OR <delibID>)` for private-delib recomputes. `reputation.Weigher.UpdateFromRound` gains `delibID string` + `isPrivate bool` parameters; private rounds emit scoped edges, skip `IncrementSurvivedCounts` (so a private-ring Sybil graduation attack cannot pump `survived_count`), and skip the global eigenvector recompute. `Weigher.WeightsFor` reads `types.DelibFromContext(ctx)` and computes per-delib EigenTrust on-the-fly over the scoped partition union'd with the global graph — the private cohort sees the global trust landscape as prior (so a seasoned agent carries their reputation in) plus intra-delib patterns, but never another private delib's edges. Cold-start cap stays load-bearing: `survived_count` is global, and private rounds don't increment it, so fresh Sybils coordinating inside a private ring cannot cold-start out of the cap. MVP skip at `service.go:1275` removed — the service now always calls `UpdateFromRound`, threading `deliberationID` + `(d.Visibility == "private")` through. New `types.DelibContext` + `types.WithDelibContext` / `types.DelibFromContext` helpers (placed in `types/delibctx.go` to break what would be a `deliberation → reputation → analysis → deliberation` import cycle). Rolling-deploy caveat: the v5 PK change from `(from, to)` to `(from, to, delib_id)` is NOT rolling-safe; a ~30s maintenance window is required because machine-1-on-v5 drops the old PK and machine-2-on-v4 code would then error on `ON CONFLICT (from_agent, to_agent)`. Accepted at current deployment scale.
- **Pubkey-bound reputation identity**: schema v4 migrates the reputation vertex from symbolic `agent_id` to a namespaced form — `"key:<agent_keys.id>"` for agents with an active registered key at emission time, `"id:<agent_id>"` otherwise. New `store.ResolveVertices(ctx, agents)` method returns per-agent vertex strings via one batched `unnest` + `LEFT JOIN agent_keys` query; `LoadReputation` invokes it internally so callers still pass symbolic names. `Weigher.UpdateFromRound` pre-resolves every symbolic agent appearing in the round (crux authors, agreers, dispute filers) and emits edges against the resolved vertices, so edges are pinned to the key active at write time — a later rotation cannot retroactively transfer attribution. New `store.VertexKeyPrefix` and `store.VertexIDPrefix` constants expose the reserved-prefix convention; the `Store` interface adds `ResolveVertices` so the in-memory test fake and the Postgres implementation share the contract. Defeats the rename attack: re-registering an existing `agent_id` under a new pubkey yields a new `agent_keys.id`, a new vertex, and cold-start reputation — the incoming principal cannot inherit the prior key's score. Legit key rotation is symmetric (rotation = reset), which is the correct defense against a compromised K1 transferring trust to a freshly-registered K2 under the same symbolic name. Unsigned deployments are unchanged: all vertices fall back to `"id:<agent>"` and behaviour matches v3. Transition semantics: registering a key for a previously-unsigned agent forks the identity into a new key-bound vertex; the prior `"id:"` row remains in the DB but is orphaned because there is no cryptographic attestation that the unsigned history belongs to the new key. One-time migration cost: pre-v4 `agent_reputation` and `agent_trust_edges` rows are **deleted** on schema bump — a clean-slate transition is safer than in-place PK rewriting under rolling deploy (where machine-1-on-v4 writes and machine-2-on-v3 writes would collide on primary key when the migration UPDATE fires). Production gemot had no deployed reputation state at time of bump; demo and test runs regenerate reputation organically. Config-free — key binding activates automatically when signatures are present; no new env knobs.
- **Hardening ships as default-on; marketing copy + landing reflect it**: closes the last contradiction with the feedback_darpa_always_on rule. **Config defaults flipped** so every deployment gets the hardening without configuring anything: `GEMOT_EIGENTRUST_ENABLED=true` (was false), `GEMOT_EIGENTRUST_DB_FAIL=closed` (was open — reputation DB outages now fail closed instead of silently stripping the Sybil defense), `GEMOT_EIGENTRUST_DECAY_HALFLIFE_DAYS=30` (was 0 — pumped-up edges now fade on a 30-day half-life), `GEMOT_EIGENTRUST_EDGE_FLOOR=0.01` + `GEMOT_EIGENTRUST_EDGE_CAP=10.0` (were 0 — trust graph row count now bounded, per-edge damage from repeated mutual endorsement clamped). **Envelope middleware** now defaults to `advisory` mode (was `off`) — unsigned requests still pass, signed requests get verified and log mismatches, so any client that starts signing gets immediate verification without breaking unsigned clients. `ParseEnvelopeMode("")` returns `EnvelopeAdvisory`; typo fallback also advisory instead of off. **Nonce cache** defaults to Postgres (was memory) so multi-instance Fly deploys have replay protection out of the box; explicit `GEMOT_NONCE_STORE=memory` still honored for single-process dev. **Landing page** gains a new "Why it's trustworthy" section after the flow one-liner: four approachable value-props (tamper-evident action log, Sybil-aware trust, signed actions + replay protection, cross-model sanity check) with plain-language framing — no DARPA jargon leaks into user-facing copy. **README** "Integrity checks" section now covers the tamper-evident log, Sybil-aware trust weights, and envelope signing + replay protection in user-friendly terms. **Robust aggregation** (Track 1 planned defense) shipped: new `trimmedMean` helper + `validateAggregationStability` integrity check in `internal/analysis/integrity.go` emits `AGGREGATION_DRIFT` warnings when a position's raw mean vote diverges from its trimmed mean by ≥ 0.2 on the -2..+2 scale. Requires ≥ 10 voters; catches the single-outlier-in-moderate-cohort scenario where a coordinated minority at the extremes pulls the aggregate away from the majority. Wired into the analysis pipeline alongside existing `validateVoteSimilarity` + `validateModelDiversity`. 4 unit tests (trim math + skip + no-false-flag on sybil majority + fires on real skew). **Subgraph-scoped trust-edge load**: new `LoadTrustEdgesForCohort(ctx, delibID, cohort)` method on the reputation Store interface returns a bounded subgraph (private partition + one-hop global neighborhood of the cohort) instead of the full graph. `Weigher.weightsForPrivateDelib` now uses this — bounds per-recompute cost to O(|cohort| × avg-degree) instead of O(|V|+|E|), removing the perf ceiling documented in THREAT_MODEL past ~10k edges. Postgres implementation uses a single query with `ANY($text[])` filters; in-memory fake store mirrors the semantics. **CSP header** added to the `secureHandler`: `default-src 'self'; script-src 'self' 'unsafe-inline'; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data:; connect-src 'self'; frame-ancestors 'none'; base-uri 'self'; form-action 'self'`. Defense-in-depth alongside the existing X-Frame-Options / X-Content-Type-Options / Referrer-Policy / Permissions-Policy headers. Inline scripts/styles are still permitted because the landing page + `/try` templates embed onclick handlers + `<style>` blocks; tightening to nonces is a future pass once the static pages are rewritten.
- **Client-side audit proof verification (session 5d, closes the BFT arc)**: the tamper-evident log can now be independently verified by clients, closing the self-attestation gap where the server previously reported its own log without anything external to cross-check. **New primitives**: `bft.VerifyAggregateSignature(roster, signers, msg, agg)` extracted as a pure function (refactored out of `BLSSigner.VerifyAggregate` — signer now delegates); `bft.VerifyQC(qc, roster)` is the client-facing helper that reconstructs `voteDigest(qc.View, qc.BlockHash)` and runs the pairing check against the listed signers' public keys. `Engine.PublicKey()` returns the replica's compressed-G2 public key (96 bytes) via the internal BLSSigner. `Service.ReplicaPublicKey()` exposes this at the service layer; new MCP action `admin action:replica_pubkey` returns `{public_key_hex, algorithm, usage}` so a client can fetch once and cache. **AuditLogEntry.Proof** field ships the JSON-encoded QC alongside each log entry — clients iterate the log, deserialize each proof, and call `bft.VerifyQC(qc, roster)` to confirm offline that every recorded action was signed by the server. Tampered entries (corrupted view, flipped bits in the aggregate) fail verification loudly. **Acceptance**: `TestClientVerifiesAuditProofs` drives SubmitPosition + Vote + Commit, fetches the replica pubkey via the service API, pulls the audit log, and verifies each proof via the pure-function path; a negative-control check confirms a tampered QC rejects. README + `admin` tool description updated with the new action and an approachable one-liner ("Fetch the server's public key once... verify proofs offline with any BLS12-381 library — so the guarantee doesn't depend on trusting the server's report of its own log"). This is the intended stopping point for the BFT arc: the tamper-evident claim now has teeth, and any further investment (multi-node, HTTPTransport, DKG) is a meaningful operational jump that doesn't pay off for single-user deployments.
- **Tamper-evident action log, always on (meshing 5a–5c into the default path)**: removes the last feature-flag-shaped hole in the BFT work so every user benefits without knowing the word "BFT". `NewService` now auto-constructs an in-memory `bft.Engine` (InMemoryLogStore + InMemoryVoteHistoryStore + InMemoryReplicaKeyStore) so the BFT-routing code path is always exercised; `main.go` overrides with the Postgres-backed engine via `SetBFTEngine`. The `if s.bftEngine != nil` branch in `SubmitPosition` is gone — the engine is never nil. Three additional write paths now route through BFT: `castVote`, `Commit`, `DisputeCrux`. Each calls a new `Service.orderAction(ctx, opType, parts...)` helper that canonicalizes the payload as `op|field1|field2|…` and submits through the engine BEFORE the domain-table write, so the log is the source of truth for "did this action happen" and a failed BFT append refuses the domain write. `types.Position.BFTProof` removed — clients never saw raw QC bytes in practice, and leaving them in confused the API without adding value; the QC is recoverable server-side from `bft_log`. New `Service.GetTamperEvidentLog(ctx, deliberationID)` reads the engine's log store, filters to a given deliberation by parsing the pipe-delimited payload, and returns `[]AuditLogEntry{Height, View, ActionType, AgentID, BlockHash}`. `admin action:get_audit_log` gains a `tamper_evident_log` field alongside the existing `operations` + `analysis_decisions` — one call returns all three views of a deliberation's history. README updated with one approachable line under Integrity checks ("Every write is ordered through an append-only cryptographic log... verify the server hasn't quietly rewritten history"), matching the house style that keeps DARPA jargon out of the user-facing surface. Acceptance: `TestWritesRoutedThroughBFT` drives SubmitPosition + Vote + Commit against a Postgres-backed engine and confirms bft_log grows; all 286 existing tests continue to pass under the auto-constructed in-memory engine, confirming no behavioral regression.
- **HotStuff SMR session 5c — BLS keypair persistence (DARPA Track 1 Byzantine-tolerant sequence agreement, session 5c of N)**: closes the cross-boot Submit gap that session 5b left open. **New**: `internal/bft/keystore.go` defines `ReplicaKeyStore` interface with `LoadOrGenerate(ctx, replicaID) (BLSKeypair, error)` — atomic first-boot generate-and-store, subsequent-boot read-through; `InMemoryReplicaKeyStore` reference impl with per-ID caching. `internal/bft/bls_signer.go` adds `BLSKeypair.Marshal()` (32-byte canonical fr.Element scalar + 96-byte compressed G2) and `UnmarshalBLSKeypair(priv, pub)` that verifies `priv*g2 == pub` before returning, so a tampered stored pair fails at load rather than producing signatures no one can verify. `internal/store/bft_replica_keys.go` ships `PostgresReplicaKeyStore` implementing `bft.ReplicaKeyStore` against the new schema-v8 `bft_replica_keys` table. `LoadOrGenerate` is race-safe: the fast-path SELECT returns existing rows; on miss, a candidate is generated and inserted via `ON CONFLICT (replica_id) DO NOTHING`, then the row is re-read to return whichever concurrent caller won the race. `internal/store/schema.sql` adds `bft_replica_keys` (replica_id TEXT PK, private_key BYTEA, public_key BYTEA, created_at TIMESTAMPTZ) and bumps schema to version 8; migration is additive. `internal/store/store.go` bumps `expectedVersion` to 8. **Bootstrap signature change**: `bft.BootstrapSingleNode` now takes a fourth argument, `keyStore ReplicaKeyStore`; a nil keyStore falls back to an in-memory store (test-only, reproduces pre-5c fresh-per-boot behavior). `main.go` constructs `store.NewPostgresReplicaKeyStore(db)` and passes it to Bootstrap. **Tests**: `tests/bft_service_integration_test.go` gains `TestReplicaKeyStoreReturnsSameKey` (persistence + replica-id isolation) and, critically, restores `TestBFTEngineResumesAcrossBoot` — drives 3 Submits under the first engine, constructs a second engine against the same stores, and extends the chain via another Submit. The post-restart Submit verifies the prior-boot QC (impossible in 5b because BLS keys regenerated); the test now passes. Existing `TestSubmitPositionRoutedThroughBFT` updated to thread the new keystore through. **Security note**: `bft_replica_keys.private_key` is the replica's signing authority — treat access to this column like any other secret. Session 5d+ may migrate to file-backed or HSM-backed key storage if operational posture demands; for single-node demo deployment the DB-stored key is equivalent to the Postgres DSN itself. **Docs**: `specs/hotstuff-design.md` + `THREAT_MODEL.md` updated. Still deferred: route votes + analyze close + commitments through BFT (session 5d), client-side QC verification helper + MCP tool to expose replica pubkey (5d/5e), multi-node Fly deploy with `HTTPTransport` + real wall-clock timer (5e+), replica-key distribution via trusted setup or DKG for multi-node, formal `<>[]progress` liveness in TLA+.
- **HotStuff SMR session 5b — service-layer integration (DARPA Track 1 Byzantine-tolerant sequence agreement, session 5b of N)**: wires the BFT package into `main.go` + `deliberation/service.go` so position submissions actually flow through the HotStuff state machine. **New**: `internal/bft/engine.go` defines a single-node `Engine` that serializes propose → self-vote → QC formation under a mutex; returns the prepared QC (the block commits when the next Submit's justify carries it, per HotStuff's two-chain rule — quiescent systems leave the last block uncommitted, documented). Advances view explicitly after each Submit since the protocol layer does not auto-advance on QC formation. `RestoreChainState` primes the engine's parent/justify pointers from the replica's replayed committed log so a restart continues the chain instead of forking genesis. `EncodeQCProof` JSON-encodes a QC for client-facing responses. `internal/bft/bootstrap.go` ships `BootstrapSingleNode(ctx, log, voteHist)` — generates a fresh BLS keypair, constructs a single-replica cluster (roster=[0]), attaches log + vote history, calls Replay and RestoreVoteHistory, and advances view past the prior-boot `proposedInView` so a Submit after restart does not collide with an already-proposed view. `internal/bft/state.go` gains a `HighQC()` getter so the engine can read the freshly-formed QC after HandleVote's internal processJustify advance. **Service wiring**: `Service` struct gains `bftEngine *bft.Engine` (optional — nil disables routing, for tests not running the full binary); `SetBFTEngine(e)` setter. `SubmitPosition` builds a canonical payload `submit_position|<delib-id>|<agent-id>|<round>|<content>` and calls `engine.Submit` BEFORE writing to the positions table; the returned QC is JSON-encoded and attached to `Position.BFTProof`. `types.Position` gains `BFTProof []byte \`json:"bft_proof,omitempty"\`` so MCP/A2A response shapes surface the proof to clients. `main.go` constructs the engine at startup using `store.NewPostgresLogStore(db)` + `store.NewPostgresVoteHistoryStore(db, ReplicaID(0))` + `bft.BootstrapSingleNode`; startup fails loudly if the bootstrap errors. **Tests**: `internal/bft/engine_test.go` ships 2 unit tests — single-node Submit returns view-1 QC over the proposed block; two-chain commit fires on the SECOND Submit and block 1 lands in the log (before the second Submit, log is empty). `tests/bft_service_integration_test.go` ships 2 end-to-end Postgres integration tests — `TestSubmitPositionRoutedThroughBFT` drives two SubmitPosition calls through the service with a live engine attached, decodes the `BFTProof` QC on each position (views 1 and 2, 1 signer at single-node), and confirms the committed log reflects the two-chain commit; `TestBFTEngineBootIdempotent` drives 3 Submits to seed committed blocks, then constructs a second engine against the same stores (fresh replica, same Postgres stores) and verifies `Bootstrap` succeeds without mutating the committed log — exercises the `Replay + RestoreVoteHistory + AdvanceView(proposedInView+1)` composition that prevents the post-restart double-propose error. **Known session-5b limitation**: cross-boot Submit (extending the chain after restart) currently fails because `BootstrapSingleNode` generates a fresh BLS keypair on every boot — QCs signed under the prior boot's key cannot be verified under the new roster. The committed log survives restart (block contents, heights, parent pointers); only QC verification breaks. This lands with session 5c (persisted BLS key + client-side QC verification — both require stable per-replica pubkey). For single-node demo posture this is acceptable: each boot is effectively its own chain after the first restart. Documented in `specs/hotstuff-design.md` + `THREAT_MODEL.md`. **What routes through BFT**: only `SubmitPosition` in session 5b. Votes, analysis round close, commitment creation, disputes, and other writes still take the direct-Postgres path. Extending routing incrementally per-operation (rather than big-bang) keeps blast radius tight — each additional op is a small diff that reuses the same engine. **Acceptance**: (1) `SubmitPosition` returns a non-empty `BFTProof` when the engine is attached; (2) the JSON-encoded QC in `BFTProof` is a well-formed `bft.QC` (View, BlockHash, Signers, AggSig); (3) committed log monotonically accrues; (4) restart against existing log succeeds without errors; (5) no regressions in the existing test suite. **Docs**: `specs/hotstuff-design.md` status line + previous-sessions summary rewritten; `THREAT_MODEL.md` sub-caveat updated to reflect routing. Still deferred to session 5c+: BLS key persistence (required for cross-boot + client-side QC verification), multi-node Fly deploy with `HTTPTransport` + real wall-clock timer, routing votes + analyze + commitments through BFT, formal `<>[]progress` liveness in TLA+, replica-key distribution via trusted setup or DKG.
- **HotStuff SMR session 5a — durable vote history (DARPA Track 1 Byzantine-tolerant sequence agreement, session 5a of N)**: closes the session-4 anti-equivocation gap by persisting the `lastVotedView` and `proposedInView` counters across restarts. **New**: `internal/bft/votehistory.go` defines `VoteHistoryStore` interface (SaveVote/SaveProposal/Load) with `InMemoryVoteHistoryStore` reference implementation enforcing monotonic writes. `internal/bft/state.go` gains a `voteHistory VoteHistoryStore` field plus `SetVoteHistory` (attach-only) and `RestoreVoteHistory(ctx, vh)` (load persisted counters into a fresh replica, then attach) setters. `internal/bft/protocol.go`: `HandleProposal` calls `SaveVote(p.View)` BEFORE updating `r.lastVotedView` and emitting the vote — on persist failure the replica refuses to vote (liveness loss, not safety loss). `Propose` calls `SaveProposal(r.view)` BEFORE updating `r.proposedInView` and returning the proposal for broadcast. Reversing this order would let a crash-after-broadcast leave the replica free to equivocate on restart; the current order means a crash-before-broadcast costs only liveness (the next leader takes over via view change). `internal/store/bft_vote_history.go` ships `PostgresVoteHistoryStore` implementing `bft.VoteHistoryStore` against the new `bft_vote_history` table. Writes use `ON CONFLICT (replica_id) DO UPDATE SET ... = GREATEST(existing, new)` so a stale retry with a lower view cannot regress the persisted value. Load returns `(0, 0)` for replicas that have never written, matching the in-memory default. Multi-replica isolation via `replica_id` primary key — two replicas sharing the same database (tests, single-process multi-replica deploys) stay independent. `internal/store/schema.sql` bumps to version 7 with the `bft_vote_history` table (replica_id TEXT PK, last_voted_view BIGINT, last_proposed_view BIGINT, updated_at TIMESTAMPTZ). Migration is additive: empty table has zero runtime cost. `internal/store/store.go` bumps `expectedVersion` to 7. **Tests**: `internal/bft/votehistory_test.go` adds 5 unit tests — in-memory roundtrip; regression rejection (lower view after higher view errors; same-value idempotent); `HandleProposal` persists `lastVotedView`; `Propose` persists `proposedInView`; `RestoreVoteHistory` blocks a double-vote scenario where a pre-populated store simulates a prior boot and the fresh replica refuses to vote in the already-voted view. `tests/bft_vote_history_integration_test.go` adds 2 Postgres integration tests — roundtrip with monotonic UPSERT verification + replica-id isolation; end-to-end restore-blocks-double-vote against real Postgres. All 30 bft package tests pass (13 protocol + 7 BLS unit + 5 log/replay + 5 vote-history); all 4 Postgres integration tests pass. `specs/hotstuff-design.md` promotes the durable-vote-history deferred item to delivered as item 3a; `THREAT_MODEL.md` sub-caveat updated to reflect the closed safety gap. **Additive — internal/bft/ remains not wired into service.go**; the bft_vote_history table is created on migration but stays empty in prod until session 5b wires the service layer. Still deferred to session 5b+: service-layer integration, multi-node Fly deploy with HTTPTransport + real wall-clock timer, client-side QC proof verification, replica-key distribution via trusted setup or DKG, formal `<>[]progress` liveness in TLA+.
- **HotStuff SMR session 4 — durable commit log (DARPA Track 1 Byzantine-tolerant sequence agreement, session 4 of N)**: ships the persistence layer for HotStuff-committed blocks. **New**: `internal/bft/log.go` defines `LogStore` interface (Append/Load/HighestHeight) with an `InMemoryLogStore` reference implementation enforcing height-monotonic append, gap rejection, and fork detection via new `ErrLogForkDetected` (different-hash at existing height must escalate as a safety violation — idempotent exact-match re-append is fine). JSON `EncodeBlock`/`EncodeQC` helpers for storage format (MVP debuggability; session 5 may migrate to deterministic binary if size matters). `internal/bft/replay.go` defines `Replay(r *Replica, log LogStore)` that reconstructs `knownBlocks`, `committed`, `committedLog`, `view`, `highQC`, `lockedQC`, and `preparedQC` on a fresh Replica — callers invoke before driving any protocol messages. `internal/bft/state.go` gains a `log LogStore` field plus `SetLog` setter. `internal/bft/protocol.go` `commitBlock` refactored to take `(Block, QC)` and persist log-first-then-memory: the in-memory state never advances past the persisted tail. If the log append fails, `commitBlock` returns an error and the caller (via `processJustify` → `HandleProposal` or `HandleVote`) halts rather than diverging. `processJustify` now returns `error`; all four call sites propagate. `internal/store/bft_log.go` ships `PostgresLogStore` implementing `bft.LogStore` against the new `bft_log` table. Append uses `INSERT ... ON CONFLICT DO NOTHING` + a post-insert hash compare to distinguish idempotent re-insert from a fork (the latter surfaces as `ErrLogForkDetected` so the replica halts rather than silently picking the first writer). `internal/store/schema.sql` bumps to version 6 with the `bft_log` table definition (height BIGINT PK, block_hash BYTEA, block_bytes BYTEA, qc_bytes BYTEA, committed_at TIMESTAMPTZ DEFAULT NOW()) plus a block-hash index for future reverse lookups. Migration is additive: empty `bft_log` has zero runtime cost on existing deployments. `internal/store/store.go` bumps `expectedVersion` to 6 so a post-v6 binary running against a pre-v6 DB warns appropriately. **Tests**: `internal/bft/log_test.go` adds 5 unit tests — append/load roundtrip with HighestHeight; fork-detection (different block at same height rejected, identical block at same height idempotent); gap rejection (height-3 with no height-2 errors); log writes on protocol commit (4-round cluster with shared in-memory log yields 3 entries h1/h2/h3 — the round-4 leader commits h3 via the session-2 HandleVote→processJustify integration, not just h1/h2 as a naive read would expect); Replay reconstructs committed state (drive 4 rounds with logging, spin up fresh replica, Replay from shared log, verify committed log contains genesis + h1 + h2 + h3 in that order and view = 4). `tests/bft_log_integration_test.go` adds 2 Postgres integration tests — append/load roundtrip with HighestHeight, fork detection via the ON CONFLICT + post-insert compare path. All 25 bft package tests pass (13 protocol + 7 BLS unit + 5 log/replay); all 2 Postgres integration tests pass. **Known session-4 gap** — `lastVotedView` and `proposedInView` are NOT persisted. A crashed-and-restarted replica can re-emit a vote or proposal in a view it previously committed to, equivocating under a Byzantine peer that races the restart. Safe for single-honest-replica crash recovery (current deployment posture); session 5 adds a durable vote-history side-table before multi-node deploy. **Docs**: `specs/hotstuff-design.md` promotes durable log from deferred → delivered, documents the log-first-then-memory ordering rationale and the `lastVotedView` gap; `THREAT_MODEL.md` sub-caveat updated. **Additive — internal/bft/ remains not wired into service.go**; the bft_log table is created on migration but stays empty in prod until session 5 wires the service layer. Single-node BFT mode (`GEMOT_BFT_ENABLED` feature flag) and per-operation QC proofs to clients also land in session 5 (deliberate scope split so session 4 ships a pure-persistence checkpoint without production-semantics risk).
- **HotStuff SMR session 3 — real BLS threshold sigs (DARPA Track 1 Byzantine-tolerant sequence agreement, session 3 of N)**: replaces the sessions-1/2 `PlaceholderSigner` with real BLS12-381 multi-signatures via `github.com/consensys/gnark-crypto@v0.20.1`. New `internal/bft/bls_signer.go` (~200 lines) implements the `Signer` interface on top of gnark-crypto's BLS12-381 primitives: `Sign` computes `s·H(msg)` on G1 with RFC 9380 hash-to-G1 under DST `BLS_SIG_BLS12381G1_XMD:SHA-256_SSWU_RO_NUL_`; `Verify` runs `PairingCheck([-H(msg), sig], [pk, g₂])`; `Aggregate` sums per-replica G1 points into a single 48-byte aggregate regardless of signer count; `VerifyAggregate` sums the listed signers' G2 public keys and runs one pairing check. Duplicate signers in `VerifyAggregate` rejected. Each replica owns an independent keypair — **this is BLS multi-signature, not threshold BLS**, intentionally: multi-sig matches HotStuff's QC data model exactly (QC carries the explicit `Signers` list so the verifier derives the aggregate public key by summing those signers' PKs) and avoids the DKG dependency that threshold BLS requires. Threshold / DKG is session-5 territory (alongside multi-node key distribution). Library choice finalized: gnark-crypto's curve primitives (G1/G2 arithmetic, pairing, hash-to-G1) are clean and the crypto-dependency graph grows by only one module beyond what gnark-crypto itself pulls. Verified `CGO_ENABLED=0 go build ./...` passes — pure-Go, matches the Fly Dockerfile posture; rules out herumi/bls-eth-go-binary and supranational/blst. Candidates considered and rejected: circl/sign/bls (full BLS API but VerifyAggregate takes distinct-message pairs, suboptimal for same-message HotStuff QCs); drand/kyber-bls12381 (has threshold primitives we don't yet need). New test file `internal/bft/bls_signer_test.go` exercises 7 unit tests: sign/verify roundtrip, tampered-sig rejection, wrong-signer rejection, aggregate roundtrip at 2f+1, duplicate-signer-in-VerifyAggregate rejection, aggregate-verify-rejects-wrong-message, keypair-roster-mismatch-rejection at constructor. `PlaceholderSigner` + `NewPlaceholderSigner` deleted from `signature.go` — only the `Signer` interface and `newViewDigest` helper remain (digests are message-layer, orthogonal to the signing scheme). `newCluster` test helper generates a fresh BLS keyset per cluster and distributes matched (keypair, roster) pairs to each replica; all 13 protocol tests (9 session-1 happy-path + 4 session-2 adversarial) pass under real pairing-based verification with the expected ~200ms runtime cost (up from ~3ms under placeholder). `specs/hotstuff-design.md` promotes real-sigs from deferred to delivered with the scheme details + multi-sig-vs-threshold distinction documented; `THREAT_MODEL.md` sub-caveat updated. **Still deferred**: service-layer integration + durable Postgres log (session 4), multi-node Fly deploy + HTTPTransport + real wall-clock timer + client QC proof (session 5), replica-key distribution via trusted setup or DKG (session 5), formal `<>[]progress` liveness property in TLA+. Commit is additive — no schema change, no service change, no prod deploy required; `internal/bft/` remains not wired into `internal/deliberation/service.go`. Dependency bump: `go` 1.25.0 → 1.25.9 (required by gnark-crypto v0.20.1's `go >= 1.25.7` floor).
- **HotStuff SMR session 2 — view change (DARPA Track 1 Byzantine-tolerant sequence agreement, session 2 of N)**: extends `internal/bft/` with the canonical HotStuff view-change protocol, preserving safety under Byzantine leader failure. **Go protocol additions**: new `Replica.Timeout()` constructs a domain-0x03-signed `NewView(view+1, highQC)` and broadcasts it to all peers; new `Replica.HandleNewView(nv)` verifies the sender signature and the carried highQC (via `verifyQC`, rejecting forged-QC NewViews with `ErrBadNewViewQC`), accumulates per (targetView, sender) in a new `pendingNewViews` map, and on 2f+1 collection selects the highest-view QC among the collected set and stores it in `viewChangeHighQC` for the new leader. `Propose` transparently substitutes `viewChangeHighQC[r.view]` for the caller-supplied `(parent, justify)` so post-timeout proposals automatically extend the correct branch — callers don't need to distinguish happy-path vs post-view-change. `HandleVote` now calls `processJustify` on the formed QC so the leader integrates its own output into highQC (without this, a post-vote timeout would carry a stale highQC and undermine the view-change selection rule). New error sentinels `ErrStaleNewView`, `ErrBadNewViewSig`, `ErrBadNewViewQC`. New digest domain byte `0x03` for NewView signatures, separated from vote (`0x01`) and proposal (`0x02`) so a real BLS scheme in session 3 cannot replay one domain's signature as another. **TLA+ spec additions (specs/HotStuff.tla)**: new state variables `newViews` (set of `[sender, view, highQCblock]` messages) and `timedOut` (`[Replicas -> SUBSET Views]`); new actions `TriggerTimeout(r, claimedQC)` (Byzantine can claim any QC'd block as highQC, modeling stale-NewView attacks without signature forgery) and `ViewChange(v)` (advance global view when 2f+1 NewViews exist for `v`); new liveness branch in `HonestCanVote` (vote for `b` if `b.parent.view > BlockOf(locked[r]).view`, even if `b` doesn't extend `locked[r]` — without this, view change would leave honest replicas locked on the pre-timeout branch forever); new invariant `NewViewQCIsReal` pinning the no-forged-QC threshold-sig assumption; new temporal property `ViewMonotonic` (view counter never regresses); `NewViewsMonotonic` for NewView append-only-ness; `FormQC` hardened with `v = view` guard on the view-advance clause so belatedly-formed stale-view QCs cannot regress the global view counter. Tight cfg (`HotStuff.cfg`: MaxView=2, MaxBlocks=3) explores ~14,000 states in ~2s; stress cfg (`HotStuff_stress.cfg`: MaxView=3, MaxBlocks=3) explores ~537,000 states in ~10s — both pass all safety invariants. **Adversarial test suite (internal/bft/adversarial_test.go, 4 new tests)**: `TestLeaderStall` exercises the canonical view-change flow — leader of view 1 silent, replicas 1/2/3 timeout, new leader (replica 1) collects 2f+1 NewViews and proposes in view 2 with QC forming normally; `TestLeaderEquivocatesAcrossViews` confirms safety under a Byzantine leader that broadcasts conflicting blocks to different subsets — neither block accumulates 2f+1 votes so no QC forms and no honest replica commits a height-1 block; `TestPartitionedMinority` confirms non-progress when only 2 replicas participate (sub-quorum NewView count does not populate `viewChangeHighQC`), and the progress-on-heal path once a third replica rejoins; `TestStaleNewView` verifies the highest-view selection rule — after a happy-path view-1 QC plus a partial view-2 proposal that propagates the QC into honest replicas' highQCs, two honest timeouts + one Byzantine-forged NewView carrying a stale genesis highQC result in the new leader selecting the real view-1 QC, not genesis. All 9 existing session-1 tests continue to pass; total 13 tests in `internal/bft/` running in ~3ms. **Docs**: `specs/hotstuff-design.md` promotes view change from deferred to delivered and updates the deferred list to sessions 3+ (real BLS, service integration, durable log, multi-node deploy, client proof, formal temporal liveness); `specs/README.md` adds session-2 invariants + properties + Byzantine model expansion; `THREAT_MODEL.md` "Byzantine-tolerant sequence agreement" row sub-caveat updated to reflect view-change + adversarial tests shipping with remaining deferrals listed. **Still deferred**: real threshold signatures (session 3 swaps PlaceholderSigner for BLSSigner via gnark-crypto), service-layer integration + durable Postgres log (session 4), multi-node Fly deploy + real wall-clock timeout timer + client-side QC proof verification (session 5), formal `<>[]progress` liveness property in TLA+ (deferred until bounds permit TLC liveness-check in budget). Commit is additive — no schema change, no service change, no prod deploy required; `internal/bft/` remains not wired into `internal/deliberation/service.go`.
- **HotStuff session 1 — review-driven fixes (same commit series)**: adversarial review of the session-1 drop caught a meaningful spec-vs-impl disconnect plus several medium-severity code gaps. Fixes applied before downstream work. **Spec tightening**: `LockOn` was an optional TLA+ action with no fairness, so TLC explored executions where honest replicas never locked; `ExtendsChain(b.id, locked[r])` with `locked[r]=genesis` is trivially satisfied, meaning the spec allowed honest replicas to vote for conflicting blocks across views. At the original tight bounds the violation wasn't reachable; at `MaxView=4, MaxHeight=2, MaxBlocks=5` TLC finds the explicit counterexample — `r0` commits `{0, 1, 3}` with blocks 1 and 3 both at height 1. Fix: couple the honest-replica lock update into `FormQC` atomically so every honest replica's `locked` advances monotonically on QC formation. Removed the optional `LockOn` action (now redundant). Re-verified at both tight (`HotStuff.cfg`, 510 states, <1s) and stress bounds (new `HotStuff_stress.cfg`, `MaxView=4`, 62,017 states, ~5s). The stress cfg is retained as a regression harness. **Proposal signature verification**: `HandleProposal` now rejects proposals whose `Sender` is not the designated leader for the proposal's view (`ErrWrongLeader`) and verifies the proposal's signature against that leader (`ErrBadProposalSig`). Without these checks, any entity with transport access could inject proposals claiming to be the leader — session-1 happy-path tests wouldn't have caught it because the harness is trusted. **Digest domain separation**: vote and proposal digests are now prefixed with distinct domain bytes (`0x01` vote / `0x02` proposal) so a signature over `(view, blockhash)` cannot be replayed across message types under a real threshold-sig scheme. **Double-propose guard**: `Propose` now refuses to emit a second proposal in the same view (`ErrDoublePropose`). The session-1 test harness doesn't exercise this, but it's the leader-side mirror of `lastVotedView`. **`commitBlock` chain-gap refusal**: if an ancestor is missing from `knownBlocks`, commit is refused rather than creating a gap in `committedLog`. Prevents `CommittedChainConsistent` violation under session-2 log-replay. **`pendingVotes` GC on `AdvanceView`**: buckets from strictly prior views are dropped so a long-running replica doesn't accumulate memory from views that never reached quorum. **`HandleProposal` ordering**: move `knownBlocks[h] = block` after the safety check so unsafe proposals leave no residue. **`PlaceholderSigner` replica-ID width**: encoded ID moved from 1 byte to 4 bytes (big-endian uint32); the 256-replica cap silently folded larger IDs under modulo. **Leader rotation**: changed from `view % N` to `(view-1) % N` so view 1 picks `roster[0]` rather than `roster[1]` — conventional form, doesn't affect safety. **Cleaned `TestHappyPathSingleCommit`**: deleted the smoke test with stranded dev-comments and no assertions; coverage preserved by `TestHappyPathCommitsAfterFourRounds`. **Three new tests** in `internal/bft/protocol_test.go`: `TestDoubleProposeBlocked` (leader refuses second proposal in same view), `TestWrongLeaderRejected` (non-leader sender rejected), `TestBadProposalSigRejected` (tampered signature rejected).
- **HotStuff SMR session 1 (DARPA Track 1 Byzantine-tolerant sequence agreement, partial)**: new `internal/bft/` package implements the chained-HotStuff protocol core — `types.go` defines Block/QC/Proposal/Vote/NewView message types; `state.go` holds per-replica state (view, lockedQC, highQC, preparedQC, lastVotedView, knownBlocks, committedLog) under a per-replica mutex; `protocol.go` implements `HandleProposal` / `HandleVote` / `Propose` / `AdvanceView` with the HotStuff §4 safety rule (vote iff block extends lockedQC OR justify.View > lockedQC.View) and the two-chain commit rule (QC on child C whose parent B has `C.View == B.View + 1` triggers commit of B); `signature.go` provides `PlaceholderSigner` wrapping a `byte(ReplicaID) || sha256(msg)` pseudo-signature that is EXPLICITLY non-cryptographic — session-2 work wires real threshold signatures via a pure-Go BLS library (gnark-crypto preliminary choice, enforced by the Fly Dockerfile's `CGO_ENABLED=0` posture which rules out herumi/bls-eth-go-binary and supranational/blst); `transport.go` defines the `Transport` interface plus `InMemoryTransport` (channel-backed, for tests) and an `HTTPTransport` stub that returns errors on every method so accidental use is caught immediately. New `specs/HotStuff.tla` + `HotStuff.cfg` formally model the protocol: 6 state variables (view, blocks, votes, qcs, locked, highQC, committed), 5 actions (Propose, CastVote, FormQC, LockOn, Commit), safety invariants (NoDoubleVoteInView, AgreementAtHeight — the core Yin et al. §5 Theorem 1 property, CommittedChainConsistent, QCRequiresQuorum, CommitRequiresQC, ByzantineBound), and append-only temporal properties. TLC 2.19 on OpenJDK verifies at `N=4, F=1, MaxView=2, MaxHeight=2, MaxBlocks=3` — 7,862 distinct reachable states in ~1 second, no invariant violations. `specs/README.md` is updated with a HotStuff section matching the Deliberation spec's structure, including the same projection/cutoff generalization argument. New `specs/hotstuff-design.md` is the design document: adversary model (N replicas, f<N/3 Byzantine, partially synchronous network), protocol choice rationale (HotStuff over PBFT for linear message complexity + forward-compat with Track 1 verifiable tally), message types, state machine, commit rule, storage design (log as source of truth, Postgres as projection — session 2+), which gemot operations need ordering (writes yes, reads no), the BLS library decision, and an explicit "not in session 1" list (view change, real threshold sigs, service integration, log-replay, multi-node deploy, client-side proof verification, Byzantine adversarial tests). `internal/bft/protocol_test.go` ships 7 happy-path tests — TestHappyPathSingleCommit (one commit sequence), TestHappyPathCommitsAfterFourRounds (4-round pipelined commit), TestSafetyAgreementAtHeight (pairwise height-consistency), TestQuorumThresholdAtTwoFPlusOne (QC forms at 2f+1=3 not 2), TestDoubleVoteRejected (anti-equivocation per view), TestPipelinedCommitOrdering (committed log is chain-ordered), TestByzantineBoundConstructor (NewReplica rejects f≥N/3). **The BFT package is NOT wired into `internal/deliberation/service.go`**; production gemot still runs on the single-server path. Commit is additive — no schema change, no service change, no prod deploy required.
- **THREAT_MODEL.md updated**: "Byzantine-tolerant sequence agreement" moved from Planned → Partially Implemented (session 1 of N) with an explicit deferred-items list so the DARPA bid language does not misrepresent partial BFT as production BFT.
- **THREAT_MODEL.md updated**: "Edge-table pruning + cumulative-weight caps" and "Per-deliberation privacy boundary for trust edges" moved from planned → implemented; subgraph-scoped `LoadTrustEdges` remains Planned. "Per-delib private EigenTrust" promoted to Implemented and merged into the privacy-boundary row with the MVP caveat removed.
- **THREAT_MODEL.md updated**: "Pubkey-bound reputation identity" moved from planned → implemented.
- **THREAT_MODEL.md updated**: "Durable nonce cache for multi-instance envelope verification" moved from planned → implemented.
- **Discoverability + drift-prevention pass**: agent-card.json moved from a hand-maintained static file to a Go-rendered handler (`internal/mcp/agent_card.go`) that reads `Version` directly — eliminates the entire class of version drift that left the published agent card at 0.5.0 through six minor releases. The new handler also gates by HTTP method (rejects non-GET/HEAD with 405 + `Allow` header), emits a strong ETag for cheap revalidation, and honors `If-None-Match` (including the `*` wildcard and comma-separated lists per RFC 7232 §3.2) — replacing the prior `http.FileServer` behavior which accepted any method and emitted no ETag. Skills list rewritten to cover every grouped tool action; `TestAgentCardActionCoverage` scrapes server.go's case statements and asserts each action name appears as a token (word-bounded) in some skill description. Streamable-HTTP transport declared first, legacy SSE second. README install section now describes actual server behavior: anonymous use is free for everything, plus one free `analyze:run` per deliberation in sandbox mode, with `claude mcp add` examples and a one-line activation prompt. server.json enriched with `websiteUrl`, both transport entries, and a keyword-rich description for registry search.
- **Drift tests + scripts + hooks**: new tests in `internal/mcp/agent_card_test.go` enforce three classes of drift — `TestAgentCardVersion` and `TestServerJSONVersion` assert the agent card and registry submission file match the `Version` constant, and `TestChangelogHasCurrentVersion` asserts the CHANGELOG documents the current release. `scripts/bump-version.sh` updates `server.go` + `server.json` in lockstep with semver validation and post-write verification (agent-card.json is now Version-bound so it doesn't need bumping). `scripts/verify-deploy.sh` hits prod's `/health` and `/.well-known/agent-card.json` and asserts both report the local Version — catches the "forgot to fly deploy" path. `scripts/audit-prod-surfaces.sh` is the comprehensive variant that also probes the MCP `initialize` handshake, the public MCP registry, and the HTTP status of every linked page. `scripts/githooks/pre-commit` runs CodeScene delta + a CHANGELOG-touched nudge + a hard-fail version-drift check on every commit; `scripts/install-githooks.sh` wires `core.hooksPath` to the tracked location.
- **CI: markdown link checker**: new `.github/workflows/markdown-links.yml` + `.github/markdown-link-check.json` runs on PR, on push to main, and weekly Monday on cron — catches dead docs, dead external links, and rotted citations as they happen rather than during the next release audit.
- **Fix: Fly proxy SSE buffering**: the SSE keepalive wrapper now pre-flushes a priming comment + sets `X-Accel-Buffering: no` before delegating to the SDK handler, so the SDK's `event: endpoint` establishment message reaches the client immediately instead of sitting in Fly's proxy buffer until the connection eventually closes (which had been logging as "could not finish reading HTTP body from instance" several times per minute). Verified locally that the SDK works in isolation; the gap was specifically in how Fly proxies SSE responses below their flush threshold.
- **Demo mode (no-DB run)**: gemot now boots into a fully functional in-memory mode when `DATABASE_URL` is unset — `docker run gemot/gemot` and `./gemot http` work out of the box without any external dependencies. New `internal/store/memory.go` implements the full `deliberation.Store` interface plus the gemot-specific persistence surface (audit log, async-job tracking, sandbox cleanup, LLM cache, agent keys); a compile-time assertion holds both `*store.DB` and `*store.MemoryStore` to the same `store.Backend` interface so they can't drift. `RunHTTP` now takes the `Backend` plus a nullable `*sql.DB` — the latter is used only by genuinely-Postgres-only paths (credit store, BFT-Postgres stores, `/metrics`, Stripe checkout/webhook, Postgres nonce cache), all of which are skipped or swapped for in-memory equivalents in demo mode. Boot prints a clear banner explaining the trade-off (ephemeral state, no auth required) and how to upgrade. The Dockerfile's `EXPOSE 8080` makes the container's serve port discoverable. Modern MCP clients (Claude Code, Cursor, etc.) can connect to `http://localhost:8080/mcp` against a vanilla `docker run` and exercise the full pipeline (create deliberation, submit positions, vote, analyze if `ANTHROPIC_API_KEY` is set, read the audit log) — same code paths as production, just with maps and slices behind the interface instead of Postgres.

### Tests

- New `tests/eigentrust_test.go` (6 tests): empty graph, uniform-no-edges, star-graph convergence (hub dominates), pre-trust-seeded Sybil starvation (closed ring loses under seeded teleport), pre-trust-seed boosts the seeded vertex, pure-sink no-mass-leak.
- New `tests/reputation_weigher_test.go` (7 tests): cold-start cap clamps newcomers to exactly `ColdCap`; graduation is monotonic within a single cohort call; `UpdateFromRound` accumulates agreement edges and increments `survived_count` without self-endorsement when ≥2 distinct non-self agreers sign off; duplicate `AgreeAgents` within a crux are deduped so repeated listings cannot double-count; a 2-Sybil pair ring cannot graduate either participant because neither meets the distinct-agreer threshold; Sybil ring with pumped scores but zero survived validations is starved by the cold-start cap; disabled-config yields nil weigher.
- New `tests/reputation_integration_test.go` (2 tests, Postgres): store-layer batched UPSERT roundtrips (`IncrementSurvivedCounts`, `AccumulateTrustEdges`, `PersistEigenTrustScores`, `LoadReputation`, `LoadTrustEdges`) against real Postgres schema — insert + ON CONFLICT DO UPDATE paths both covered, `survived_count` preservation across score upserts verified; full end-to-end round→reputation→next-round flow verifies `UpdateFromRound` from round 1 cold-caps all agents, and round 2 (threshold=2) graduates alice's weight above `ColdCap` while bob/carol remain cold.
- New `tests/cross_family_consistency_test.go` (5 tests): disabled-path leaves `SecondaryLLM` nil and incurs zero calls; full-agreement between primary and secondary emits no warning and exactly one secondary call per sampled crux; majority sign-flip (3/4 agents) emits `CROSS_FAMILY_DRIFT` with model + provider + truncated claim in the message; single-agent noise (25% flip) stays under the strict-majority threshold and does not false-positive; `sampleK=2` caps secondary calls at 2 even with 4 candidate cruxes (budgets per-analysis secondary cost). Uses `llm.SecondaryFunc` closure-based mock so the full drift-sample → re-score → compare pipeline runs without real Gemini HTTP.
- Two new cases in `tests/reputation_weigher_test.go` cover the fail-mode toggle: `TestWeightsForFailsClosedOnDBError` asserts that `DBFail="closed"` propagates a wrapped store error and returns nil weights; `TestWeightsForFailsOpenOnDBError` confirms the default mode still swallows errors into unit weights.
- New `tests/reputation_decay_test.go` (7 tests, unit): dispute creates a negative edge on a previously-empty pair; a dispute from an agent who previously endorsed nets out to `1 - DisputeWeight` on the same edge; self-disputes drop (no self-edge); disputes referencing unknown crux claims are silently dropped; `DecayTrustEdges` is invoked exactly once per `UpdateFromRound` when `DecayHalfLifeDays > 0`; `DecayTrustEdges` is skipped when halflife=0 (default); and the key §3 defense demonstration — a 3-Sybil mutual-endorsement ring plus one cross-group `s1 → honest` edge flips ordering under six matching disputes, with `honest` now scoring higher than the ring's best member because the one surviving positive channel is the only open mass conduit. Three new Postgres integration cases in `tests/reputation_integration_test.go` cover the real SQL: `TestDecayTrustEdgesPostgres` seeds one fresh and one 14-day-old edge, runs `DecayTrustEdges(7d)`, and asserts the fresh edge is preserved while the stale one decays to ~0.25× (two half-lives); `TestApplyDisputeEdgesPostgres` covers both INSERT (new negative row) and ON CONFLICT (subtract from existing positive) branches; `TestUnprocessedDisputesGatingPostgres` verifies the `rep_processed_at` round-to-round idempotency.
- New `tests/reputation_retention_test.go` (5 Postgres tests, DARPA Track 1 Priority 3): floor-below-pruning (0.02 edge aged 14d at halfLife=7d decays to ~0.005 and gets DELETEd by floor=0.01 while a 1.0 edge decays to ~0.25 and survives); floor=0 preserves legacy retention-off semantics; 20 consecutive +1 endorsements with cap=10 clamp at exactly 10.0 instead of summing to 20.0; cap=0 preserves legacy unbounded-accumulation semantics; disputes ignore cap (20 caps + 50 disputes end at weight=-40). Two interface-change fixes migrate existing Postgres integration tests + the in-memory `fakeReputationStore` to the new `AccumulateTrustEdges(edges, cap)` / `DecayTrustEdges(halfLife, floor)` signatures.
- New `tests/deliberation_privacy_test.go` (3 Postgres tests, DARPA Track 1 Priority 4): private-delib full round closes with zero `agent_trust_edges` rows in the GLOBAL partition (`deliberation_id=''`) and non-zero rows in the scoped partition (`deliberation_id=<delib.ID>`), plus zero `survived_count > 0` rows; public-delib regression ensures the default visibility still populates the global partition; link-visibility delib emits edges (treated as public for reputation). Each test wires `reputation.NewWeigher` end-to-end, submits 4 positions, votes across all agents, and runs `Analyze` through a `repAnalyzer` that populates `SourcePositionIDs` + `AgreeAgents` so the edge-emission path is actually exercised.
- New `internal/bft/protocol_test.go` (7 tests, happy-path only): single-shot commit sequence, four-round pipelined commits (heights 1 and 2 committed via two-chain rule), pairwise height-agreement across honest replicas, QC threshold at 2f+1 (verified both directions — 2 votes produce no QC, 3 votes produce one), double-vote rejection (`ErrDoubleVote` fires on equivocating proposals in the same view at the same replica), pipelined-commit ordering (committed log is chain-connected from genesis), and the constructor's f<N/3 safety guard. Uses `PlaceholderSigner` (session-1 non-crypto placeholder) and `InMemoryTransport` (channel-backed). Byzantine/adversarial tests are session-2 work per the deferred list in `specs/hotstuff-design.md`.
- New `tests/private_eigentrust_test.go` (4 Postgres tests, DARPA Track 1 Priority 4 FULL): `TestPrivateDelibEmitsScopedEdges` asserts the private-delib end-to-end flow writes to the scoped partition and NOT the global one; `TestPublicDelibEmitsGlobalEdges` is the mirror regression; `TestPrivateDelibEigenTrustIsolated` seeds three edges (one in delib A's partition, one in delib B's, one global) and verifies `LoadTrustEdges(ctx, "delib-A")` returns A's + global but not B's (and symmetrically for B), while the global load excludes both scoped partitions; `TestPrivateDelibInheritsGlobalReputation` exercises the cold-cap × private-path interaction — a seasoned agent (survived_count ≥ threshold) in a private delib receives above-cold-cap weight, while a fresh agent in the same private delib is cold-capped at exactly `ColdCap`, confirming the private-ring Sybil graduation attack is denied.
- New `tests/reputation_pubkey_binding_test.go` (6 Postgres tests): the rename attack — alice earns pumped reputation bound to K1, rotates to K2, and subsequent `WeightsFor("alice")` cold-caps at 0.1 instead of inheriting the prior score; unsigned agents resolve to the `"id:<agent>"` vertex and full round-trip through `IncrementSurvivedCounts` / `LoadReputation` works; unsigned-to-signed transition orphans prior symbolic reputation (no silent merge into the key-bound vertex); mixed cohorts with some signed + some unsigned agents resolve per-agent in one batched call; revoked keys fall back to `"id:<agent>"` without reassigning the prior key vertex's rep; `UpdateFromRound` emits edges with `"key:<id>"` endpoints for signed agents and `"id:<agent>"` endpoints for unsigned agents in the same round. All existing reputation tests' fake-store seeds and assertions migrated to vertex-form keys (new `idV`/`keyV` helpers).
- New `tests/adversarial_byzantine_test.go` (8 tests, DARPA Track 1 Task 5): full-pipeline Byzantine adversarial suite against real Postgres with reputation enabled. `f<1/4` (4 agents, 1 Sybil) passes with honest/Sybil effective-weight ratio = 3.00; `f=1/3` (6 agents, 2 Sybils) passes at the theoretical limit with ratio = 2.00; `f>1/2` (4 agents, 3 Sybils) documents the failure mode — attacker majority dominates, but cold-cap still bounds Sybil aggregate weight; cold-start flooding (10 fresh Sybils vs 2 seasoned legit) verifies Sybil reputation-weight sum = n × ColdCap exactly; reputation-laundering documents the no-decay gap (`survived_count` is sticky); crux-poisoning confirms `LOW_EFFORT_ABS`/`COVERAGE` warnings fire and notes the cold cap only protects synthesis, not extraction; graduation-cliff proves a 3-Sybil mutual-endorsement ring defeats `minDistinctAgreers=2` in 2 rounds, motivating future decay work; reframing-attack logs that reputation is orthogonal to framing defenses (cross-family consistency / Task 4 is the intended countermeasure). New `METRICS.md` captures the measured ratios for DARPA bid citation.
- New `tests/pg_nonce_cache_test.go` (4 tests): first-observation accepts, duplicate rejects with `ErrReplay`, janitor-sweeps-expired-rows, concurrent-safe across two independent cache instances backed by the same DB.

## v0.10.2 (2026-04-17) — A2A envelope

### New Features

- **A2A envelope signing + per-action signatures**: New `A2AAuthMiddleware` in `internal/mcp/a2a.go` lifts bearer-token auth out of the inline handler path into a proper middleware that sets `ContextKeyKeyID`/`ContextKeyIsAdmin`/`ContextKeyAPIKey` on the request context. This lets the envelope middleware's `scopeAgentID` rewrite resolve the scoped stored key during hosted-mode verification. `gemot/participate`'s `submit_position` and `vote` now accept an optional `signature` param and thread through `SubmitPositionWithSigningID` / `SubmitSignedVoteWithSigningID`, mirroring MCP's B1.5 path. Envelope middleware ordering is `A2AAuthMiddleware(envelope(handler))`, same as `/mcp`.
- **THREAT_MODEL.md updated**: "Signatures over the A2A transport" moved from open → implemented.

### Fixes

- **A2A `expert_panel`/`follow_up` credit refund**: Refund path called `creditStore.AddCredits(keyID, ...)` with the 8-char key prefix, but `AddCredits` looks up by `WHERE key = $2` and expects the full `gmt_` token. The error was swallowed with `_, _ =` so customers silently lost their credits on any upstream failure. Now uses `token` with a `gmt_` guard. Pre-existing bug surfaced during the A2A review.

### Tests

- New `tests/a2a_envelope_test.go` (14 tests): off-mode pass-through, required rejects unsigned, valid signature, bad signature, replay, advisory unsigned passes, advisory invalid rejects, auth middleware missing/bad bearer with JSON-RPC error, auth rate-limit rejection, hosted-mode scoped key with ContextKeyKeyID injection, signature param round-trip through real A2AHandler, partial headers, stale timestamp.

## v0.10.1 (2026-04-17) — Track 1 follow-ups

### New Features

- **Phase B1.5 — hosted-mode signature fix**: `scopeAgentID` in the MCP transport used to rewrite `"alice"` → `"<keyID>:alice"` before the service layer, which broke signature verification in hosted mode (client signs `"alice"`; server reconstructed the payload with the scoped form). New `SubmitPositionWithSigningID` / `SubmitSignedVoteWithSigningID` service methods decouple the stored agent_id from the signing agent_id; MCP threads the unscoped form through.
- **Task A extension — `CRUX_INSTABILITY` wired end-to-end**: `TextAnalyzer.StabilityCheckSamples > 1` now issues N extra same-prompt crux calls per subtopic and judges them against the chosen crux via a Haiku-grade semantic-same prompt. Cruxes with <2/3 semantic agreement emit `CRUX_INSTABILITY`. Opt-in via `GEMOT_STABILITY_SAMPLES` env var.
- **Phase B2 — request-envelope signing + replay protection**: new `internal/mcp/envelope.go` HTTP middleware verifies optional ed25519 signatures over `(agent_id, method, body_hash, nonce, timestamp)` on `/mcp` and `/a2a`. Three modes (`off` default / `advisory` / `required`) via `GEMOT_ENVELOPE_MODE`. `±5 min` timestamp window; in-memory `MemoryNonceCache` per-process (single-instance). New domain tag `gemot/v1/envelope` in `internal/auth`.
- **THREAT_MODEL.md updated**: all three items marked implemented; three new open items tracked (durable nonce cache for multi-instance, per-action signing over A2A, cross-family model consistency).

### Tests

- 287 tests (up from 286 after reconciling `validateCruxStability` with the wired path). New coverage:
  - Hosted-mode per-action signature path (`TestHostedModeSigningID`)
  - `cruxStabilityWarning` wired path (`TestCruxStabilityWarning_Wired`)
  - Envelope canonical payload + domain separation + sign/verify round-trip
  - Replay window validation (zero-skew, past skew, future skew)
  - In-memory nonce cache (first observation, replay rejection, TTL expiry, capacity eviction)
  - Envelope middleware: off / advisory / required modes, advisory-valid-passes, advisory-invalid-rejects, bad sig, stale timestamp, replay, missing key, partial headers, non-POST bypass, body-size limit, hosted-mode scoped-key lookup, hosted-mode missing-context rejection

## v0.10.0 (2026-04-17)

### New Features — DARPA Track 1 foundation

- **LLM-specific integrity checks** (`internal/analysis/integrity.go`): `LOW_EFFORT_ABS` / `LOW_EFFORT_REL` (absolute floor <2 claims, median-relative <25% when median ≥4), `THIN_PROVENANCE` (crux backed by <2 source positions or <2 quotes), `CRUX_INSTABILITY` hook (pure-Go; candidate generator + semantic judge unwired pending LLM-defense runs), cross-family consistency stub.
- **Per-agent ed25519 signed positions and votes**: new `internal/auth` package with SSH/Noise/TUF-style length-prefixed canonical payloads and domain separation (`gemot/v1/position`, `gemot/v1/vote`). Signatures verified against registered public keys at submit time.
- **Per-deliberation signature policy**: `none` / `advisory` / `required`, DMARC-style staged migration. DB-enforced via CHECK constraint.
- **Agent key registration and rotation**: new `agent_keys` table, `participate action:register_key` and `revoke_key` MCP tool actions, in-transaction rotation with UNIQUE partial index preventing concurrent-registration races.
- **`participate` tool gained**: optional `signature` field on `submit_position` and `vote`, `register_key` and `revoke_key` actions.
- **Threat model updated**: 7 new implemented-defense rows; planned-defense table reorganized around DARPA-PS-26-09 Track 1 items.

### Tests

- 286 tests (up from 187). New coverage:
  - Signature canonicalization, domain separation, cross-domain replay
  - Signed position/vote with policy matrix (none/advisory/required)
  - Key rotation, revocation, signature verification after rotation
  - Signed content with PII sanitization (verify against raw content)
  - Low-effort position detection (absolute + median-relative)
  - Crux provenance validation

## v0.9.0 (2026-04-06)

### Breaking Changes

- **`SetTemplate` now replaces all rules** instead of merging with existing rules. Previously, switching templates preserved old template rules even when the new template defined different defaults. Now the new template's defaults fully replace the old rules.

### Security

- **14 audit findings fixed**: Metrics endpoint method restriction, CSV export quoting, SSE shutdown ordering, webhook idempotency (partial unique index), schema version tracking, A2A job tracking, resolution lock cleanup on delete.
- **Dev mode hardening**: Rate limiting (30/min per IP) when running without `GEMOT_API_SECRET`. Auth bypass in A2A, SSE, and metrics endpoints for dev mode.
- **Atomic credit operations**: Replaced mutex + separate SELECT with `UPDATE...RETURNING` for AddCredits, Deduct, AddCreditsByEmail. Eliminates TOCTOU race conditions.
- **Webhook idempotency**: `AddCreditsByEmail` now rejects duplicate session IDs, preventing double-crediting from webhook retries.
- **Docker Postgres localhost-only**: `docker-compose.yml` binds Postgres to `127.0.0.1` instead of all interfaces.

### New Features

- **Expert panel tool**: `analyze action:expert_panel` creates a deliberation with domain-specialized expert critiques in a single call. Source types: `code_review`, `architecture`, `experiment`, `proposal`. Custom experts via JSON.
- **Service-level audit logging**: All write operations (create, submit, vote, commit, delete) logged via `SetAuditLogger` callback. Full audit log included in deliberation exports.
- **Priority API semaphore**: 7 background + 3 interactive-reserved slots for LLM calls. Expert panels and follow-up analyses get priority access, preventing starvation behind long-running batch jobs.
- **Incremental crux detection**: Multi-round analysis skips re-extracting claims from prior rounds. Persisted `ExtractedClaims` carry forward, reducing analysis time proportionally.
- **Commitment accountability**: Conditional commitments, fulfillment tracking, and agent reputation scores.
- **Position metadata**: Extensible JSON metadata field on positions. Used by diplomacy integration for lat/lon coordinates.
- **Schema version table**: Warns on startup if database schema is ahead of binary version (downgrade detection).

### Performance

- **Parallel topic analysis**: Topics run in parallel (summaries + crux detection share bounded semaphore of 5). Previously sequential — 3x speedup for multi-topic deliberations.
- **CheckParticipantCap**: Single `COUNT(DISTINCT agent_id)` query replaces loading all positions into memory for max_participants enforcement.
- **Resolution deferred to analysis**: Resolution recalculated after analysis completes, not on every vote. Reduces per-vote overhead.

### Improvements

- **Context propagation**: All 42 Service methods accept `context.Context`. 90 `context.TODO()` calls eliminated. Request cancellation and deadlines propagate from HTTP handlers through to database queries.
- **SSE reliability**: Robust reconnect (max 10 retries, 3s backoff), server IdleTimeout increased to 10 minutes.
- **Graceful deploys**: Rolling strategy with 10-minute drain timeout. SSE clients notified before HTTP server shutdown.
- **Parallel claim extraction**: 6 concurrent goroutines for claim extraction.
- **LLM response caching**: 24h TTL, SHA256 keys.
- **CSV export**: Position IDs and timestamps now quoted to prevent injection.

### Documentation

- **Getting Started tutorial**: End-to-end walkthrough of first deliberation.
- **Environment variables reference**: All config vars documented in README.
- **Stale references fixed**: SQLite references updated to Postgres throughout docs.

### Tests

- 187 tests (up from 161). New coverage:
  - Audit logger callback verification
  - Participant cap enforcement and existing-agent bypass
  - Concurrent credit deductions (race condition tests)
  - Webhook idempotency (double-credit prevention)
  - Resolution update after analysis
  - Soft-delete and resolution lock cleanup

## v0.8.0 (2026-04-04)

- Grouped MCP tools (6 tools with `action` parameter, down from 30+)
- Postgres migration (from SQLite via pgx)
- Resolution layer with template-aware consensus thresholds
- Governance templates: assembly, sortition, parliament, jury, consensus, negotiation, review
- Delegation (liquid democracy), withdrawal, deadlines
- A2A JSON-RPC endpoint with MCP/A2A method parity
- Structured logging (slog)

## v0.7.0

- Enriched agent context (cluster, allies, disagreements, diversity nudge)
- Multi-scope deliberations
- Security hardening

## v0.5.0

- Streamable HTTP transport
- Multi-use join codes
- XSS fix
- Sandbox mode
