/gemot
/gemot-server
/t3c-import
/expert-panel
/main
.gemot/
*.db
.env
__pycache__/

# defn database
.defn/
.codex/
.agent-key.pem
.mcp-registry-key.pem
.plancheck-debug.json
defn-adoption-feedback.md
PRODUCT_FEEDBACK.md
mcp-publisher
.mcp.json
/calendar-scheduling
/diplomacy

# TLA+ model-checker scratch dir
specs/states/

# Local-only hook experiments (active hooks path is scripts/githooks/)
.githooks/

# Raw t3c source data (pre-anonymization) — only committed outputs go in
integrations/t3c/AI Manifestos-*.json

# Calibration run outputs (transient experiment artifacts; the embedded
# snapshot at internal/calibration/embed/latest.json is the source of truth)
/calibration_*.json

# Private notes: session hand-off docs, draft specs, brainstorm artifacts
# that may or may not graduate into the repo when something ships.
.notes/
